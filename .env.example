# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=123456:ABC-your-token-here

# Reactive Resume API key: rxresu.me → Settings → API Keys → Create a new API key
RXRESUME_API_KEY=your-rxresume-api-key

# API base URL. Cloud default below; for self-hosted use https://<host>/api/openapi
RXRESUME_BASE_URL=https://rxresu.me/api/openapi

# Optional: saved AI provider id (rxresu.me → Settings → AI) used to parse PDFs.
# Leave empty to use the account default.
RXRESUME_AI_PROVIDER_ID=

# LLM used to adapt the resume to a job vacancy
OPENAI_API_KEY=sk-your-key-here
# Optional: for OpenRouter use https://openrouter.ai/api/v1 (auto if key starts with sk-or-)
OPENAI_BASE_URL=
# Model. For OpenRouter GPT-5 use openai/gpt-5
LLM_MODEL=gpt-4o-mini
# Reasoning effort for reasoning models (gpt-5): minimal|low|medium|high.
# Leave empty for non-reasoning models. gpt-5 needs this set (e.g. low) so hidden
# reasoning doesn't consume the whole reply budget.
OPENAI_REASONING_EFFORT=

# /vacancy cover letter tuning
COVER_LETTER_TEMPERATURE=0.55
COVER_LETTER_MAX_TOKENS=900
COVER_LETTER_GOLDEN_LIMIT=3

# Max accepted PDF size in MB (Telegram bot download limit is ~20 MB)
MAX_PDF_SIZE_MB=15
